interface:
  display_name: "Unslop"
  short_description: "Remove AI tells from every authored response"
  default_prompt: "Use $unslop to make this writing direct, specific, and human."
policy:
  allow_implicit_invocation: true
