interface:
  display_name: "Commit"
  short_description: "Create scoped local git commits"
  default_prompt: "Use $ce-commit to commit only the changes from this task with a clear message."
policy:
  allow_implicit_invocation: true
