interface:
  display_name: "Plan"
  short_description: "Create grounded Markdown implementation plans"
  default_prompt: "Use $ce-plan to create a grounded, implementation-ready Markdown plan for this work."
policy:
  allow_implicit_invocation: true
