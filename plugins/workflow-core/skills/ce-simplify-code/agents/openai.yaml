interface:
  display_name: "Simplify code"
  short_description: "Simplify settled code without behavior changes"
  default_prompt: "Use $ce-simplify-code to simplify this settled change while preserving exact behavior."
policy:
  allow_implicit_invocation: true
