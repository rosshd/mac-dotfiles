interface:
  display_name: "Resolve PR feedback"
  short_description: "Judge and resolve existing PR comments"
  default_prompt: "Use $ce-resolve-pr-feedback to evaluate and resolve the feedback on this PR."
policy:
  allow_implicit_invocation: true
