interface:
  display_name: "Work"
  short_description: "Implement and locally verify concrete changes"
  default_prompt: "Use $ce-work to implement and locally verify this concrete request."
policy:
  allow_implicit_invocation: true
