interface:
  display_name: "Blast radius"
  short_description: "Prove hidden effects beyond a code diff"
  default_prompt: "Use $blast-radius to identify and prove what this change could break elsewhere."
policy:
  allow_implicit_invocation: false
