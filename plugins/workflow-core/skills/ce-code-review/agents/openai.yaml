interface:
  display_name: "Code review"
  short_description: "Find concrete defects and regressions"
  default_prompt: "Use $ce-code-review to review this change for actionable defects and missing coverage."
policy:
  allow_implicit_invocation: true
