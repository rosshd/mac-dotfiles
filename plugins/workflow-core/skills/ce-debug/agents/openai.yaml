interface:
  display_name: "Debug"
  short_description: "Reproduce failures and prove their root cause"
  default_prompt: "Use $ce-debug to reproduce and diagnose this failure, then fix it only if requested."
policy:
  allow_implicit_invocation: true
